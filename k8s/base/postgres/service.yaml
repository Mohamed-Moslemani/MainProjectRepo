apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: docflow
  labels: { app: postgres }
spec:
  clusterIP: None   # headless — StatefulSet pattern
  selector: { app: postgres }
  ports:
    - name: pg
      port: 5432
      targetPort: pg

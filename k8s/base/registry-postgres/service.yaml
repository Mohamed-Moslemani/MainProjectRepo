apiVersion: v1
kind: Service
metadata:
  name: registry-postgres
  namespace: docflow
  labels: { app: registry-postgres }
spec:
  clusterIP: None
  selector: { app: registry-postgres }
  ports:
    - name: pg
      port: 5432
      targetPort: pg

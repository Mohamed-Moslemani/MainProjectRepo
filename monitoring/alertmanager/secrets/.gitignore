# Alertmanager receiver secrets — real values are environment-
# specific and rotated per-cluster. The directory must exist (the
# alertmanager container mounts it read-only); the placeholder
# files below keep the path valid in dev.
*
!.gitignore
!README.md
